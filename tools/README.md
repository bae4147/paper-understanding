# Tools 디렉토리

실험용 논문을 준비하는 도구 모음입니다.

## 📚 도구 목록

### 1. `simple_prepare.py` ⭐ (추천 - 의존성 없음)

**기능**: 이미 다운로드한 HTML 논문 파일을 실험용으로 준비

**특징**:
- 외부 라이브러리 필요 없음 (Python 기본 라이브러리만 사용)
- HTML 파일명 정리 및 복사
- 이미지 자동 복사
- 메타데이터 JSON 자동 생성

**사용법**:
```bash
cd tools
python3 simple_prepare.py "<긴_HTML_파일명.html>" paper-id
```

**예시**:
```bash
# ACM에서 다운로드한 HTML 파일 준비
python3 simple_prepare.py \
  "../papers_html/Exploring Older Adults Personality Preferences for LLM-powered Conversational Companions _ Proceedings of the Extended Abstracts of the CHI Conference on Human Factors in Computing Systems.html" \
  chi2025-older-adults-personality
```

**결과물**:
- `papers_html/{paper-id}.html` - 짧은 파일명의 HTML
- `papers_images/{paper-id}/` - 이미지 파일들
- `papers_json/{paper-id}.json` - 메타데이터 (제목, 섹션 정보)

---

### 2. `scrape_acm.py` (BeautifulSoup 필요)

**기능**: ACM Digital Library에서 논문을 직접 다운로드

**특징**:
- DOI 또는 URL 입력만으로 자동 다운로드
- 이미지, 표, 수식 모두 포함
- 섹션 자동 인식

**사용법**:
```bash
cd tools
python3 scrape_acm.py <DOI_또는_URL> [paper-id]
```

**예시**:
```bash
# DOI로 다운로드
python3 scrape_acm.py 10.1145/3706599.3719940 chi2025-lbw-01

# URL로 다운로드
python3 scrape_acm.py https://dl.acm.org/doi/10.1145/3706599.3719940 chi2025-lbw-01
```

**필요한 라이브러리**:
```bash
pip3 install beautifulsoup4 requests
```

---

### 3. `parse_acm_html.py` (BeautifulSoup 필요)

**기능**: 이미 다운로드한 ACM HTML을 파싱하여 깨끗한 실험용 HTML 생성

**특징**:
- ACM의 복잡한 HTML 구조를 단순화
- 섹션 경계 정확하게 계산
- 테이블과 이미지 모두 지원

**사용법**:
```bash
cd tools
python3 parse_acm_html.py <html_file> [paper-id]
```

**필요한 라이브러리**:
```bash
pip3 install beautifulsoup4
```

---

### 4. `convert_paper.py` (PyMuPDF 필요)

**기능**: PDF 파일을 HTML로 변환

**특징**:
- PDF에서 텍스트 추출
- 섹션 자동 인식 (키워드 기반)
- 이미지는 추출되지 않음 (텍스트만)

**사용법**:
```bash
cd tools
python3 convert_paper.py ../papers/paper.pdf [paper-id]
```

**필요한 라이브러리**:
```bash
pip3 install PyMuPDF
```

---

### 5. `integrate_papers.py`

**기능**: 준비된 논문들을 실험 시스템에 통합

**참고**: 현재 개발 중 (experiment.html 템플릿 필요)

---

## 🎯 사용 시나리오

### 시나리오 1: ACM에서 HTML 다운로드한 경우 (추천!)

1. ACM Digital Library에서 논문 페이지를 웹브라우저로 열기
2. "다른 이름으로 저장" → 전체 웹페이지 저장
3. `simple_prepare.py` 실행:

```bash
cd tools
python3 simple_prepare.py "../papers_html/긴파일명.html" short-id
```

**장점**:
- 의존성 없음
- 이미지 모두 포함
- 빠르고 간단

---

### 시나리오 2: ACM에서 직접 다운로드

```bash
cd tools
python3 scrape_acm.py https://dl.acm.org/doi/10.1145/3706599.3719940 paper-id
```

**장점**:
- 한 번에 모든 작업 완료
- 메타데이터 자동 추출

**단점**:
- BeautifulSoup 설치 필요
- 네트워크 필요

---

### 시나리오 3: PDF 파일만 있는 경우

```bash
cd tools
python3 convert_paper.py ../papers/paper.pdf paper-id
```

**장점**:
- PDF만 있어도 가능

**단점**:
- 이미지 추출 안 됨
- 텍스트 추출 품질이 PDF에 따라 다름
- PyMuPDF 설치 필요

---

## 📋 Paper ID 규칙

- **소문자만** 사용
- **하이픈(-)** 으로 단어 구분
- **공백 없음**
- **예시**: `chi2025-older-adults-personality`, `chi2025-user-control-automation`

---

## 🗂️ 생성되는 파일 구조

```
reading-experiment/
├── papers_html/
│   └── {paper-id}.html          # 실험용 HTML
├── papers_json/
│   └── {paper-id}.json          # 메타데이터 (제목, 섹션)
└── papers_images/
    └── {paper-id}/              # 이미지 파일들
        ├── image1.png
        ├── image2.jpg
        └── ...
```

---

## ❓ FAQ

**Q: BeautifulSoup 설치 오류가 나요**
A: `simple_prepare.py`를 대신 사용하세요. 외부 라이브러리가 필요 없습니다.

**Q: 섹션이 제대로 인식되지 않아요**
A: `simple_prepare.py`는 HTML의 h2 태그를 기반으로 섹션을 찾습니다. HTML 파일을 열어서 섹션 구조를 확인해보세요.

**Q: 이미지가 안 보여요**
A: `papers_images/{paper-id}/` 폴더에 이미지가 있는지 확인하세요. `simple_prepare.py`는 `{원본파일명}_files` 폴더에서 자동으로 복사합니다.

**Q: 여러 논문을 한 번에 준비할 수 있나요?**
A: 현재는 개별적으로 실행해야 합니다. 여러 논문은 순차적으로 각각 실행하세요.

---

## 💡 팁

1. **HTML 다운로드 시**: "전체 웹페이지"로 저장해야 이미지 폴더(`_files`)가 함께 저장됩니다.

2. **파일명이 너무 길 때**: `simple_prepare.py`가 자동으로 짧은 이름으로 복사합니다.

3. **메타데이터 확인**: `papers_json/{paper-id}.json` 파일을 열어서 섹션 정보가 올바른지 확인하세요.

4. **이미지 경로 문제**: 생성된 HTML은 상대 경로(`../papers_images/{paper-id}/`)를 사용합니다. 프로젝트 루트에서 실행해야 이미지가 제대로 보입니다.
